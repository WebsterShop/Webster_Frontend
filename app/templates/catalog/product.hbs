<div class="row">
    <div class="col-sm-12">
        <div class="caption">
            <h3>{{product.name}}</h3>
            <p>{{product.description}}</p>
            <div class="price-box"><p class="lead">{{product.price}} &euro;</p></div>
            <p>{{product.inventory}} pieces available</p>
            <p>
                <a href="#" class="btn btn-primary" role="button">Kaufen</a>
            </p>
        </div>
        <img {{bindAttr src="product.image"}} alt="...">
    </div>
</div>